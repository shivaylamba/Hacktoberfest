#include<bits/stdc++.h>
using namespace std;

int main()
{
	for(int i=2; i<1000; i++)
	{
		int n=i;
		int temp=0;
		
		while (n!=0)
		{
			int rem=0;
			
			rem = n % 10;
			temp += pow(rem, 3);
			n /= 10;
		}
		
		if(i==temp)
		cout<<i<<"\n";
		
	}
}
