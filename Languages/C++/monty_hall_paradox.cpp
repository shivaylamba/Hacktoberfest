#include <bits/stdc++.h>
using namespace std;

int main() {
	int n;
	cout << 1 << endl << fflush(stdout);
	cin >> n;
	if(n==2)
	    cout << 3 << endl << fflush(stdout);
	else
	    cout << 2 << endl << fflush(stdout);
	return 0;
}