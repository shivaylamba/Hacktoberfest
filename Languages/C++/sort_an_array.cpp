#include <bits/stdc++.h>
using namespace std ;
typedef long long int ll ;
int main()
{
	ios_base::sync_with_stdio(0);cin.tie(0) ;
	ll n , p;
	cin >> n >> p ;
	vector <ll> ans ;
	ll *a = new ll [n] ;
	for(ll i = 0 ; i < n ; i ++)
	{
		cin >> a[i] ;
	}
	sort(a,a+n) ;
}
